name: weather_matters
description: "Look at the weather."
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cron: ^0.6.0
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  

flutter:
  uses-material-design: true
  assets:
    - assets/sun.png
    - assets/moon.png
    - assets/apikey.txt
    - assets/robobatIcon.png
